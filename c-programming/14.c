//write a program to check if a number is divisible by 2 or not without using if else
#include <stdio.h>
int main(){
    int n;
    printf("enter a number");
    scanf("%d",&n);
    printf("number is divisible:%d",n/2==0);
}