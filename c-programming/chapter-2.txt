INSTRUCTIONS 
----------------------

this are statements in program

types :-
        1. types declaration instructions --> declare var before using it  (use and declare saath mein nhi kr sakte )


        2. arithmetic instructions  --> operand 1 oprator(+) operand 2 (single value on the left side jaha pr values jakar store hoyengi)

                pow (x,y) for x to the power y -->  #include<math.h>
                                      a=bc invalid 
                                      a=b^c invalid (^ xor operators)
                                      a=b/c valid
                                      c=b*c valid

                moduler operator (%) it is give reminder and it is not work on float values only its work over integer

                type convertion -->
                                   int op int -->int
                                   int op float -->float
                                   float op float -->float

                                implisit convertion
                                 explisit comvertion

             operators precedence 
                                 *  /  %   +  -        aur ye left to right solve hota hai

                     associativity rules (for some precedence)




        3. control instructions--> used to determine flow of program 
                                a. sequence control 
                                --------------------

                                    
                                b. decision control
                                -------------------


                                c. loop control
                                ------------------


                                d. case control
                                ------------------



OPERATORS
-----------------------------------------------

a. Arithmetic operators (+,-,*,%,/)
b. Relational Operators (== , >, <, >=,<=,!=)
c. Logical Operators    (&&, ||, !)
d. Bitwise Operators     
e. Assignment Operators  (= , +=,-=,*=,/=,%=)
f. Ternary Operators    condition? do something  if TRUE : do Something if FALSE;
g . increment  operator ++i , i++
h. decrement operator   --i , i--

priority :- ! , * , / , %,+,-,<,<=,>,>=,==,!=,&&,||,=